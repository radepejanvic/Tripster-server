package com.tripster.project.repository;

import com.tripster.project.model.AccommodationReviewReport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccommodationReviewReportRepository extends JpaRepository<AccommodationReviewReport, Long> {
}
